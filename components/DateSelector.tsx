import { formatLocalDate } from '@/lib/time';
export default function DateSelector({ dates, selected, onChange }:{dates:string[]; selected:string; onChange:(d:string)=>void}){ const shift=(n:number)=>{ const d=new Date(`${selected}T00:00:00`); d.setDate(d.getDate()+n); onChange(formatLocalDate(d)); }; return <div className="no-print flex flex-wrap items-end gap-2"><button className="rounded border bg-white px-3 py-2" onClick={()=>shift(-1)}>前日</button><div><label className="block text-xs">日付</label><input list="date-list" type="date" value={selected} onChange={e=>onChange(e.target.value)} className="rounded border px-3 py-2"/><datalist id="date-list">{dates.map(d=><option key={d} value={d}/>)}</datalist></div><button className="rounded border bg-white px-3 py-2" onClick={()=>shift(1)}>翌日</button><button className="rounded border bg-white px-3 py-2" onClick={()=>onChange(formatLocalDate(new Date()))}>今日</button></div> }
