import type { ScheduleItem } from '@/lib/types';
import { generateTimeSlots, sortByStartTime } from '@/lib/time';
import StatusBadge from './StatusBadge';
export default function TimelineTable({ items }:{items:ScheduleItem[]}){ const slots=generateTimeSlots('07:00','22:00',15); return <section className="rounded-lg border bg-white p-4 shadow-sm"><div className="mb-3 flex items-center justify-between"><h2 className="text-lg font-bold">全体タイムライン</h2><span className="text-xs text-gray-500">15分スロット: {slots[0]}〜{slots.at(-1)}</span></div><div className="overflow-x-auto"><table className="w-full min-w-[820px] border-collapse text-sm"><thead><tr className="bg-gray-100 text-left"><th className="border p-2">時間</th><th className="border p-2">職員名</th><th className="border p-2">プログラム名</th><th className="border p-2">イベント名</th><th className="border p-2">集合場所</th><th className="border p-2">行先</th><th className="border p-2">ステータス</th></tr></thead><tbody>{sortByStartTime(items).map((s,i)=><tr key={i}><td className="border p-2 font-semibold">{s.startTime}〜{s.endTime}</td><td className="border p-2">{s.staffName}</td><td className="border p-2">{s.programName}</td><td className="border p-2">{s.eventName}</td><td className="border p-2">{s.gatheringPlace}</td><td className="border p-2">{s.destination||'-'}</td><td className="border p-2"><StatusBadge status={s.status}/></td></tr>)}{items.length===0&&<tr><td colSpan={7} className="border p-6 text-center text-gray-500">該当する予定がありません</td></tr>}</tbody></table></div></section> }
