'use client';
import { useState } from 'react';
export default function PasswordGate({ onLogin }: { onLogin:()=>void }) { const [password,setPassword]=useState(''); const [error,setError]=useState(''); const [loading,setLoading]=useState(false); async function submit(e:React.FormEvent){ e.preventDefault(); setLoading(true); setError(''); const res=await fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password})}); setLoading(false); if(res.ok) onLogin(); else setError((await res.json()).error||'ログインできません'); } return <main className="flex min-h-screen items-center justify-center bg-gray-100 p-6"><form onSubmit={submit} className="w-full max-w-md rounded-lg border bg-white p-8 shadow"><h1 className="mb-6 text-2xl font-bold">職員スケジュール管理</h1><label className="block text-sm font-medium">共有パスワード</label><input type="password" className="mt-2 w-full rounded border p-3" value={password} onChange={e=>setPassword(e.target.value)} autoFocus />{error&&<p className="mt-3 text-sm text-red-700">{error}</p>}<button className="mt-6 w-full rounded bg-gray-900 px-4 py-3 font-semibold text-white disabled:opacity-50" disabled={loading}>{loading?'確認中':'ログイン'}</button></form></main>; }
