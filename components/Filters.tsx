export type FilterState={staffName:string; programName:string; status:string};
export default function Filters({staff,programs,statuses,value,onChange}:{staff:string[];programs:string[];statuses:string[];value:FilterState;onChange:(v:FilterState)=>void}){ const sel=(key:keyof FilterState, opts:string[], label:string)=><label className="text-xs">{label}<select className="mt-1 block rounded border bg-white px-3 py-2 text-sm" value={value[key]} onChange={e=>onChange({...value,[key]:e.target.value})}><option value="">すべて</option>{opts.map(o=><option key={o}>{o}</option>)}</select></label>; return <div className="no-print flex flex-wrap gap-3">{sel('staffName',staff,'職員名')}{sel('programName',programs,'プログラム')}{sel('status',statuses,'ステータス')}</div> }
